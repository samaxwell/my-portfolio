version: 0.1

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
     - echo Nothing to do yet

artifacts:
  files:
   - index.html
   - favicon.ico
   - Sean_Maxwell_Resume_Cognizant.docx
   - styles/*
   - images/*
